/* const userSchema = new mongoose.Schema({
  name: String,
  age: Number
});

const User = mongoose.model("User", userSchema);

// test insert
app.get("/add", async (req, res) => {
  const user = new User({ name: "Ahmad", age: 22 });
  await user.save();
  res.send("User Added ✅");
}); */